* Add a system to minify and package CSS assets
* Add a documentation system which builds HTML documentation (nodejs)
* Add an "autotest" feature which runs tests automatically as implementation and test files change
